# Directorio de Flujos ETL (Apache Hop)

En esta carpeta se almacenarán todos tus pipelines (`.hpl`), workflows (`.hwf`) y metadatos de configuración de entornos creados en Apache Hop Web.

*   `lib/`: Almacena drivers JDBC adicionales en caso de ser necesarios.
